<%= form_with(model: entry, local: true) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :entry_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :entry_description %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :entry_value %>
  </div>

  <div class="field">
    <%= form.label :other %>
    <%= form.text_field :other, id: :entry_other %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
